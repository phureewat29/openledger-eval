node_modules/
.env
logs/
# Reports are the regression trail and go in whole, transcripts included.
# live.json is the exception: the runner rewrites it every few seconds while a
# matrix is going, so committing it would leave the tree dirty mid-run.
reports/*/live.json
.claude
CLAUDE.md
dist/
